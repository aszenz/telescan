{
  "$schema": "../app.schema.json",
  "id": "firebase-cli",
  "name": "Firebase CLI",
  "category": "cloud-cli",
  "platforms": [
    "linux",
    "macos",
    "windows"
  ],
  "what": "Sends anonymous CLI usage to Google Analytics.",
  "default_state": "off",
  "detect": {
    "which": [
      "firebase"
    ],
    "paths": [
      "{config}/configstore/firebase-tools.json"
    ]
  },
  "checks": [
    {
      "type": "json",
      "key": "usage",
      "files": [
        "{config}/configstore/firebase-tools.json",
        "{home}/.config/configstore/firebase-tools.json"
      ],
      "disabled_when": [
        "*falsy*"
      ],
      "enabled_when": [
        "*truthy*"
      ]
    }
  ],
  "disable": {
    "steps": [
      "Set \"usage\": false in the Firebase configstore file."
    ]
  },
  "docs": "https://firebase.google.com/support/privacy"
}
